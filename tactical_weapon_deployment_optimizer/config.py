# config.py - UPDATED

# Grid generation settings
GRID_RESOLUTION_KM = 1.0  # 1km between candidate points
GRID_RADIUS_MULTIPLIER = 1.2  # Search area = max_range * this multiplier

# Output settings only (no input file dependencies)
OUTPUT_DIR = "data/output/"